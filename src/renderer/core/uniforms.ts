import type { UniformManifest } from '../../shared/manifest';
export function bindUniforms(defs:UniformManifest[], values:Record<string,unknown>):Record<string,unknown>{ const out:Record<string,unknown>={}; for(const d of defs){let v=values[d.name]??d.default; if(d.type==='float'||d.type==='int'){v=Number(v); if(d.type==='int')v=Math.round(v as number); if(d.min!==undefined)v=Math.max(d.min,v as number); if(d.max!==undefined)v=Math.min(d.max,v as number);} else if(d.type==='bool')v=Boolean(v); else if(d.type==='select'&&d.options&&!d.options.includes(String(v)))v=d.default; out[d.name]=v;} return out; }
